name: Build and Publish Releases

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Build Multi-OS Assets
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            asset_type: deb
            arch: amd64
            pkg_dir: pkg_amd64
            output_name: touch-grass-sim_1.0.0_amd64.deb
          - os: windows-latest
            asset_type: exe
            output_name: Touch-Grass-Sim-Windows-x64.zip
          - os: macos-latest
            asset_type: macos
            output_name: Touch-Grass-Sim-macOS.zip

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # --- LINUX DEBIAN BUILD ---
      - name: Build Debian Package
        if: matrix.asset_type == 'deb'
        run: |
          sudo apt-get update
          sudo apt-get install -y dpkg-dev python3-pil
          
          mkdir -p ${{ matrix.pkg_dir }}/usr/bin
          cp main.py ${{ matrix.pkg_dir }}/usr/bin/touch-grass-sim
          chmod +x ${{ matrix.pkg_dir }}/usr/bin/touch-grass-sim
          
          mkdir -p ${{ matrix.pkg_dir }}/usr/share/touch-grass-sim/assets
          mkdir -p ${{ matrix.pkg_dir }}/usr/share/icons/hicolor/256x256/apps
          
          # Force copy ALL PNGs
          cp -f anime.png ${{ matrix.pkg_dir }}/usr/share/touch-grass-sim/assets/
          cp -f ghibli.png ${{ matrix.pkg_dir }}/usr/share/touch-grass-sim/assets/
          cp -f icon.png ${{ matrix.pkg_dir }}/usr/share/touch-grass-sim/assets/
          cp -f icon.png ${{ matrix.pkg_dir }}/usr/share/icons/hicolor/256x256/apps/touch-grass-sim.png

          chmod +x ${{ matrix.pkg_dir }}/DEBIAN/postinst || true
          dpkg-deb --build ${{ matrix.pkg_dir }} ${{ matrix.output_name }}

      # --- WINDOWS EXE BUILD ---
      - name: Build Windows Executable
        if: matrix.asset_type == 'exe'
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller pynput Pillow
          # Exe bundles all three images using add-data
          pyinstaller --noconfirm --onedir --windowed --add-data "anime.png;." --add-data "ghibli.png;." --add-data "icon.png;." --name "Touch-Grass-Sim" main.py
          Compress-Archive -Path dist/Touch-Grass-Sim -DestinationPath dist/Touch-Grass-Sim-Windows-x64.zip

      # --- MACOS ZIP BUILD ---
      - name: Build macOS App Bundle
        if: matrix.asset_type == 'macos'
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller pynput Pillow
          # Mac bundles all three images using add-data with a colon ':'
          pyinstaller --noconfirm --windowed --add-data "anime.png:." --add-data "ghibli.png:." --add-data "icon.png:." --name "Touch-Grass-Sim" main.py
          cd dist && zip -r ../Touch-Grass-Sim-macOS.zip Touch-Grass-Sim.app

      - name: Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: asset-${{ matrix.os }}-${{ matrix.arch || 'sys' }}
          path: |
            *.deb
            dist/*.zip
            *.zip

  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download Artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-files
          merge-multiple: true

      - name: Publish Release
        uses: softprops/action-gh-release@v2
        with:
          files: release-files/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}